# Normalize text files to LF in the repo and on checkout
*           text=auto eol=lf

# Explicit rules (overrides)
*.py        text eol=lf
*.sh        text eol=lf
*.md        text eol=lf
*.yml       text eol=lf
*.json      text eol=lf

# Windows-only files that truly need CRLF
*.bat       text eol=crlf
*.cmd       text eol=crlf

# Binary (never touch line endings)
*.uf2       binary
*.zip       binary
*.png       binary
*.jpg       binary
*.exe       binary
